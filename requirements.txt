# =============================================================================
# requirements.txt - v6 local pipeline
# =============================================================================
# Tested with Python 3.11 - 3.13. Install with:
#     pip install -r requirements.txt
# =============================================================================

# --- Core data processing ------------------------------------------------
pandas>=2.0.0
numpy>=1.24.0

# --- HTTP client for Azure OpenAI calls ----------------------------------
httpx>=0.27.0

# --- Notebook (Jupyter / VS Code Python kernel) --------------------------
# nest_asyncio lets the pipeline run inside the notebook's existing event loop.
# Without it, you get "asyncio.run() cannot be called from a running event loop".
nest_asyncio>=1.6.0
ipykernel>=6.29.0
jupyter>=1.0.0
ipywidgets>=8.1.0

# --- Optional: Azure blob storage --------------------------------------
# Only needed if you want to read prompts / write outputs to Azure Blob storage.
# Safe to leave installed even if not using blob - just won't be imported.
azure-storage-blob>=12.19.0
azure-core>=1.30.0

# --- Optional: env var loading from .env file --------------------------
# Lets you put AZURE_OPENAI_ENDPOINT etc. in a local .env file instead of
# setting them in your shell every time.
python-dotenv>=1.0.0

# --- Optional: reading/writing Excel for ad-hoc analysis ---------------
openpyxl>=3.1.0
