Subject: RE: Pricing/ Market Assessment and payor data overview assistance

Hi Shelli,

Thanks — the attachment is helpful. Here's where AI fits against your bullets.

**Market size** — I can classify historical 911 and Nurse Navigation encounters against the ten service options to estimate addressable volume for each. Eligibility for several of them sits in unstructured narrative rather than structured fields, so AI text extraction is what makes this possible at scale.

**Benchmarks** — pricing information for services like these is spread across a lot of separate public material, and finding it manually is slow. I'd use AI to search that material, pull out anything pricing-relevant, and organize it into a single comparison table. Three things that make the output usable:

- Every figure carries its source and the surrounding context it came from, so anything in the table can be traced back and verified.
- Figures get normalized into common units — per member, per engagement, per case — so offerings priced differently can still be compared directly.
- Each entry is labeled as sourced or estimated, so it's clear which numbers are solid enough to put in front of a customer.

It's also repeatable. Once it's built, we can re-run it as new information surfaces rather than starting the research over.

**Pricing models** — the options differ in what triggers them, event-based versus payer-supplied list, which sets a different billable unit for each. I can model them against volume and cost data on that basis.

**Value drivers** — STARS and HEDIS gap closure are already measurable, so those can be sized directly from data.

Thanks,
Josh